# Quantum Climb Site

This repository now serves as the active Quantum Climb website codebase.

## Local development

Prerequisite: Node.js

1. Install dependencies with `npm install`
2. Start the Vite dev server with `npm run dev`
3. Build production assets with `npm run build`
